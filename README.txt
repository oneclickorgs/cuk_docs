This is internal documentation for Co-operatives UK.

It is designed to be viewed as a Gollum-powered wiki:

$ gem install bundler
$ bundle install
$ bundle exec gollum

then visit http://localhost:4567/ in your browser.
